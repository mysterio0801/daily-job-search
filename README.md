# daily-job-search
A daily job-match pipeline for backend/software engineers. It collects recent LinkedIn postings, hard-filters out the obvious mismatches (wrong discipline, wrong seniority, staffing aggregators), scores the survivors against your rubric with Claude, and writes a ranked, color-coded .xlsx tracker.
